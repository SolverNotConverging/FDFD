The cross-family convention regression in
`../../../../tests/test_nonmodal_time_convention.py` covers Bloch wrapping,
complex frequency branches, and passive homogeneous media. Run it from the
repository root with the installed packages.
